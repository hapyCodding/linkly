# 프론트 빌드/실행 시 참조하는 백엔드 주소.
# 로컬 개발: http://localhost:8081
# 배포(정적 호스팅 시): https://your-nas.synology.me/linkly
VITE_API_BASE=http://localhost:8081
# WebSocket 주소를 자동 유도하지 않고 직접 지정하고 싶을 때만 설정 (선택)
# VITE_WS_URL=ws://localhost:8081/ws
